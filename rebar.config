{erl_opts, [debug_info]}.
{deps, []}.

{plugins, [rebar3_format]}.

{format, [
    {files, ["src/*.erl"]},
    {formatter, otp_formatter},
    {options, #{paper => 80}}    
]}.

{profiles, [{docs,
            [{edoc_opts, [
			  {files, ["sstr.erl"]}]}
			]}]         
}.